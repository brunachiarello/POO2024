#include <stdio.h>
#include <limits.h>

int conta_inteiros(FILE *input);

int conta_inteiros(FILE *input) {
  // Coloque a sua implementacao aqui...
}

int main() {
  printf("%d\n",conta_inteiros(stdin));
  return 0;
}
